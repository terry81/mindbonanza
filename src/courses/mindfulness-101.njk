---
layout: base.njk
title: Mindfulness 101 - Complete Beginner Course
---

{% set courseData = courses['mindfulness-101'] %}

<section class="course-header">
    <div class="container">
        <div class="breadcrumb">
            <a href="/courses/">← All Courses</a>
        </div>
        <h1>{{ courseData.title }}</h1>
        <p class="course-intro">{{ courseData.description }}</p>
        <div class="course-meta-bar">
            <span>📅 {{ courseData.duration }}</span>
            <span>🎓 {{ courseData.level }}</span>
            <span>⏱️ 15-30 min/day</span>
        </div>
    </div>
</section>

{% if lesson.isOverview %}
<!-- Course Overview Page -->
<section class="course-overview">
    <div class="container">
        <div class="overview-content">
            <h2>About This Course</h2>
            <p>Welcome to Mindfulness 101! This 7-day journey will introduce you to the fundamentals of mindfulness meditation. Each day builds on the previous, giving you a solid foundation for a lifelong practice.</p>

            <div class="course-structure">
                <h3>📚 Course Structure</h3>
                <p>Each day includes:</p>
                <ul>
                    <li><strong>Teaching:</strong> Clear explanations of key concepts</li>
                    <li><strong>Guided Practice:</strong> Step-by-step meditation instructions</li>
                    <li><strong>Reflection:</strong> Questions to deepen your understanding</li>
                    <li><strong>Daily Integration:</strong> Ways to practice throughout your day</li>
                </ul>
            </div>

            <div class="lessons-overview">
                <h3>📖 7-Day Curriculum</h3>
                <div class="lessons-grid">
                    {% for dayLesson in courseData.lessons %}
                    <div class="lesson-card">
                        <div class="lesson-number">Day {{ dayLesson.day }}</div>
                        <h4>{{ dayLesson.title }}</h4>
                        <p>{{ dayLesson.type }} • {{ dayLesson.duration }}</p>
                        {% if dayLesson.day == 1 %}
                        <a href="/courses/mindfulness-101/day-{{ dayLesson.day }}/" class="start-btn">Start Here →</a>
                        {% else %}
                        <a href="/courses/mindfulness-101/day-{{ dayLesson.day }}/" class="lesson-link">View Lesson</a>
                        {% endif %}
                    </div>
                    {% endfor %}
                </div>
            </div>

            <div class="ready-section">
                <h3>Ready to Begin?</h3>
                <p>Start with Day 1 and proceed at your own pace. Consistency matters more than speed—take your time with each lesson.</p>
                <a href="/courses/mindfulness-101/day-1/" class="btn-primary btn-large">Begin Day 1 →</a>
            </div>
        </div>
    </div>
</section>

{% else %}
<!-- Individual Lesson Page -->
<section class="lesson-content">
    <div class="container">
        <div class="lesson-container">

            <div class="lesson-nav-top">
                {% if lesson.day > 1 %}
                <a href="/courses/mindfulness-101/day-{{ lesson.day - 1 }}/" class="nav-btn">← Day {{ lesson.day - 1 }}</a>
                {% endif %}
                <span class="lesson-progress">Day {{ lesson.day }} of 7</span>
                {% if lesson.day < 7 %}
                <a href="/courses/mindfulness-101/day-{{ lesson.day + 1 }}/" class="nav-btn">Day {{ lesson.day + 1 }} →</a>
                {% endif %}
            </div>

            <div class="lesson-header">
                <span class="day-badge">Day {{ lesson.day }}</span>
                <h2>{{ lesson.title }}</h2>
                <div class="lesson-meta">
                    <span>{{ lesson.type }}</span>
                    <span>•</span>
                    <span>{{ lesson.duration }}</span>
                </div>
            </div>

            {% if lesson.content.introduction %}
            <div class="lesson-section intro-section">
                <p class="introduction">{{ lesson.content.introduction }}</p>
            </div>
            {% endif %}

            {% if lesson.content.keyPoints %}
            <div class="lesson-section key-points">
                <h3>🔑 Key Points</h3>
                <ul class="key-points-list">
                    {% for point in lesson.content.keyPoints %}
                    <li>{{ point }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% endif %}

            {% if lesson.content.teaching %}
            <div class="lesson-section teaching">
                <h3>📚 Teaching</h3>
                <div class="teaching-content">
                    {{ lesson.content.teaching | safe | replace('\n\n', '</p><p>') | replace('\n', '<br>') }}
                </div>
            </div>
            {% endif %}

            {% if lesson.content.practice %}
            <div class="lesson-section practice-section">
                <h3>🧘 Today's Practice: {{ lesson.content.practice.name }}</h3>
                {% if lesson.content.practice.duration %}
                <p class="practice-duration">Duration: {{ lesson.content.practice.duration }}</p>
                {% endif %}

                <div class="practice-instructions">
                    <h4>Instructions:</h4>
                    <ol>
                        {% for instruction in lesson.content.practice.instructions %}
                        <li>{{ instruction }}</li>
                        {% endfor %}
                    </ol>
                </div>

                {% if lesson.content.practice.tips %}
                <div class="practice-tips">
                    <h4>💡 Tips:</h4>
                    <ul>
                        {% for tip in lesson.content.practice.tips %}
                        <li>{{ tip }}</li>
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}

                <div class="practice-action">
                    <a href="/assets/meditation-timer/" class="btn-primary" target="_blank">Use Meditation Timer →</a>
                </div>
            </div>
            {% endif %}

            {% if lesson.content.reflection %}
            <div class="lesson-section reflection">
                <h3>💭 Reflection</h3>
                {% if lesson.content.reflection.questions %}
                <p>After your practice, take a few minutes to reflect on these questions:</p>
                <ul class="reflection-questions">
                    {% for question in lesson.content.reflection.questions %}
                    <li>{{ question }}</li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
            {% endif %}

            {% if lesson.content.dailyIntegration %}
            <div class="lesson-section daily-integration">
                <h3>🌟 Daily Integration</h3>
                <p>{{ lesson.content.dailyIntegration }}</p>
            </div>
            {% endif %}

            {% if lesson.content.action %}
            <div class="lesson-section action-section">
                <h3>✅ {{ lesson.content.action.title }}</h3>
                <p>{{ lesson.content.action.description }}</p>
            </div>
            {% endif %}

            {% if lesson.content.completion %}
            <div class="lesson-section completion-section">
                <div class="completion-badge">🎉 Course Complete!</div>
                <h3>{{ lesson.content.completion.message }}</h3>

                {% if lesson.content.completion.keyTakeaways %}
                <div class="takeaways">
                    <h4>Key Takeaways:</h4>
                    <ul>
                        {% for takeaway in lesson.content.completion.keyTakeaways %}
                        <li>{{ takeaway }}</li>
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}

                {% if lesson.content.completion.nextSteps %}
                <div class="next-steps">
                    <h4>Next Steps:</h4>
                    <ul>
                        {% for step in lesson.content.completion.nextSteps %}
                        <li>{{ step }}</li>
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}

                <div class="completion-actions">
                    <a href="/courses/" class="btn-primary">Explore More Courses</a>
                    <a href="/courses/30-day-challenge/" class="btn-secondary">Try 30-Day Challenge</a>
                </div>
            </div>
            {% endif %}

            <div class="lesson-nav-bottom">
                {% if lesson.day > 1 %}
                <a href="/courses/mindfulness-101/day-{{ lesson.day - 1 }}/" class="nav-btn-large prev">← Previous: Day {{ lesson.day - 1 }}</a>
                {% endif %}
                {% if lesson.day < 7 %}
                <a href="/courses/mindfulness-101/day-{{ lesson.day + 1 }}/" class="nav-btn-large next">Next: Day {{ lesson.day + 1 }} →</a>
                {% else %}
                <a href="/courses/" class="nav-btn-large">Back to All Courses</a>
                {% endif %}
            </div>

        </div>
    </div>
</section>
{% endif %}

<style>
.course-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 60px 20px 40px;
}

.breadcrumb a {
    color: white;
    opacity: 0.9;
    text-decoration: none;
    font-size: 0.95em;
}

.breadcrumb a:hover {
    opacity: 1;
    text-decoration: underline;
}

.course-header h1 {
    font-size: 2.5em;
    margin: 20px 0 15px;
}

.course-intro {
    font-size: 1.2em;
    opacity: 0.95;
    max-width: 800px;
    line-height: 1.6;
}

.course-meta-bar {
    display: flex;
    gap: 25px;
    margin-top: 25px;
    font-size: 1.05em;
}

.course-overview, .lesson-content {
    padding: 60px 20px;
}

.overview-content {
    max-width: 900px;
    margin: 0 auto;
}

.overview-content h2 {
    font-size: 2.2em;
    color: #333;
    margin-bottom: 20px;
}

.overview-content h3 {
    font-size: 1.7em;
    color: #667eea;
    margin: 40px 0 20px;
}

.course-structure {
    background: #f8f9fa;
    padding: 30px;
    border-radius: 12px;
    margin: 30px 0;
}

.course-structure ul {
    margin-top: 15px;
    line-height: 2;
}

.lessons-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 20px;
    margin-top: 25px;
}

.lesson-card {
    background: white;
    border: 2px solid #e0e0e0;
    border-radius: 12px;
    padding: 25px;
    transition: all 0.3s ease;
}

.lesson-card:hover {
    border-color: #667eea;
    box-shadow: 0 5px 20px rgba(102, 126, 234, 0.15);
}

.lesson-number {
    background: #667eea;
    color: white;
    display: inline-block;
    padding: 5px 15px;
    border-radius: 15px;
    font-size: 0.9em;
    font-weight: bold;
    margin-bottom: 12px;
}

.lesson-card h4 {
    color: #333;
    margin-bottom: 8px;
    font-size: 1.2em;
}

.lesson-card p {
    color: #888;
    font-size: 0.95em;
    margin-bottom: 15px;
}

.start-btn, .lesson-link {
    display: inline-block;
    padding: 10px 20px;
    background: #667eea;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.start-btn:hover, .lesson-link:hover {
    background: #764ba2;
    transform: translateY(-2px);
}

.ready-section {
    text-align: center;
    margin: 60px 0 40px;
    padding: 40px;
    background: #f0f4ff;
    border-radius: 15px;
}

.ready-section h3 {
    font-size: 2em;
    color: #333;
}

.btn-primary.btn-large {
    display: inline-block;
    padding: 18px 45px;
    font-size: 1.2em;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    margin-top: 20px;
    transition: all 0.3s ease;
}

.btn-primary.btn-large:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
}

/* Lesson Page Styles */
.lesson-container {
    max-width: 800px;
    margin: 0 auto;
}

.lesson-nav-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
    padding-bottom: 20px;
    border-bottom: 2px solid #e0e0e0;
}

.nav-btn {
    padding: 10px 20px;
    background: #f8f9fa;
    color: #667eea;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.nav-btn:hover {
    background: #667eea;
    color: white;
}

.lesson-progress {
    color: #888;
    font-weight: 600;
}

.lesson-header {
    text-align: center;
    margin-bottom: 40px;
}

.day-badge {
    background: #667eea;
    color: white;
    padding: 8px 20px;
    border-radius: 20px;
    font-size: 0.9em;
    font-weight: bold;
    display: inline-block;
    margin-bottom: 15px;
}

.lesson-header h2 {
    font-size: 2.5em;
    color: #333;
    margin-bottom: 10px;
}

.lesson-meta {
    color: #888;
    font-size: 1.05em;
}

.lesson-section {
    margin: 40px 0;
    padding: 35px;
    background: white;
    border-radius: 12px;
    box-shadow: 0 3px 15px rgba(0,0,0,0.06);
}

.lesson-section h3 {
    color: #667eea;
    font-size: 1.7em;
    margin-bottom: 20px;
}

.intro-section {
    background: #f0f4ff;
}

.introduction {
    font-size: 1.2em;
    line-height: 1.8;
    color: #333;
}

.key-points-list {
    list-style: none;
    padding: 0;
}

.key-points-list li {
    padding-left: 30px;
    position: relative;
    margin-bottom: 12px;
    line-height: 1.7;
    color: #555;
}

.key-points-list li:before {
    content: "✓";
    position: absolute;
    left: 0;
    color: #667eea;
    font-weight: bold;
    font-size: 1.2em;
}

.teaching-content {
    line-height: 1.9;
    color: #333;
    font-size: 1.05em;
}

.teaching-content p {
    margin-bottom: 20px;
}

.practice-section {
    background: #fff8f0;
    border-left: 4px solid #667eea;
}

.practice-duration {
    color: #667eea;
    font-weight: 600;
    margin-bottom: 20px;
}

.practice-instructions ol {
    counter-reset: practice-counter;
    list-style: none;
    padding: 0;
}

.practice-instructions li {
    counter-increment: practice-counter;
    margin-bottom: 15px;
    padding-left: 40px;
    position: relative;
    line-height: 1.7;
}

.practice-instructions li:before {
    content: counter(practice-counter);
    position: absolute;
    left: 0;
    top: 0;
    background: #667eea;
    color: white;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 0.9em;
}

.practice-tips {
    margin-top: 25px;
    background: white;
    padding: 20px;
    border-radius: 8px;
}

.practice-tips ul {
    list-style-position: inside;
    line-height: 1.8;
    color: #555;
}

.practice-action {
    margin-top: 25px;
    text-align: center;
}

.reflection-questions {
    list-style: none;
    padding: 0;
}

.reflection-questions li {
    padding: 15px 20px;
    background: #f8f9fa;
    border-left: 3px solid #667eea;
    margin-bottom: 12px;
    font-style: italic;
    color: #555;
}

.daily-integration {
    background: #f0fff4;
    border-left: 4px solid #48bb78;
}

.action-section {
    background: #fff5f5;
    border-left: 4px solid #f56565;
}

.completion-section {
    background: linear-gradient(135deg, #f0f4ff 0%, #fff8f0 100%);
    text-align: center;
    padding: 50px 40px !important;
}

.completion-badge {
    font-size: 3em;
    margin-bottom: 20px;
}

.completion-section h3 {
    font-size: 1.8em;
    color: #333;
    margin-bottom: 30px;
    line-height: 1.6;
}

.takeaways, .next-steps {
    text-align: left;
    max-width: 600px;
    margin: 30px auto;
    background: white;
    padding: 25px;
    border-radius: 10px;
}

.takeaways h4, .next-steps h4 {
    color: #667eea;
    margin-bottom: 15px;
}

.takeaways ul, .next-steps ul {
    list-style-position: inside;
    line-height: 2;
    color: #555;
}

.completion-actions {
    margin-top: 35px;
    display: flex;
    gap: 15px;
    justify-content: center;
    flex-wrap: wrap;
}

.btn-primary, .btn-secondary {
    display: inline-block;
    padding: 15px 30px;
    border-radius: 10px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
}

.btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
}

.btn-secondary {
    background: white;
    color: #667eea;
    border: 2px solid #667eea;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
}

.btn-secondary:hover {
    background: #667eea;
    color: white;
}

.lesson-nav-bottom {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin-top: 50px;
    padding-top: 30px;
    border-top: 2px solid #e0e0e0;
}

.nav-btn-large {
    flex: 1;
    padding: 20px 30px;
    background: #667eea;
    color: white;
    text-decoration: none;
    border-radius: 10px;
    font-weight: 600;
    font-size: 1.1em;
    text-align: center;
    transition: all 0.3s ease;
}

.nav-btn-large:hover {
    background: #764ba2;
    transform: translateY(-2px);
}

.nav-btn-large.prev {
    background: #f8f9fa;
    color: #667eea;
}

.nav-btn-large.prev:hover {
    background: #667eea;
    color: white;
}

@media (max-width: 768px) {
    .course-meta-bar {
        flex-direction: column;
        gap: 10px;
    }

    .lessons-grid {
        grid-template-columns: 1fr;
    }

    .lesson-nav-bottom {
        flex-direction: column;
    }

    .lesson-header h2 {
        font-size: 2em;
    }

    .lesson-section {
        padding: 25px 20px;
    }
}
</style>
